name: Deploy Odoo Module

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy to Webdock
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}

          script: |
            cd /opt/odoo/custom-addons/waaneiza_recruitment

            git fetch origin
            git reset --hard origin/main

            # clear python cache
            find /opt/odoo -name "*.pyc" -delete
            find /opt/odoo -name "__pycache__" -type d -exec rm -rf {} +

            sudo -u odoo /opt/odoo/odoo/odoo-bin \
            -d testing2 \
            -u waaneiza_recruitment \
            --stop-after-init

            sudo systemctl restart odoo
            sudo systemctl restart nginx